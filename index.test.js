const { APP_NAME } = require('dotenv').config();
const test_lis = [];

console.log(`Testing ${APP_NAME}...`);

console.log(`${APP_NAME} Testing complete.`);
console.log(`Ran ${test_lis.length} tests.`);
